const CACHE='trip-advisor-__BUILD__';
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(['./','./index.html'])).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('trip-advisor-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET'||event.request.mode!=='navigate'||new URL(event.request.url).origin!==self.location.origin)return;event.respondWith((async()=>{try{const res=await fetch(event.request);if(!res.ok)throw Error('Unavailable');const cache=await caches.open(CACHE);await cache.put('./index.html',res.clone());return res}catch{return (await caches.match('./index.html'))||new Response('尚未下載離線頁面，請先連線開啟一次。',{status:503,headers:{'Content-Type':'text/plain;charset=utf-8'}})}})())});
