import {search} from './search.mjs';
import {html} from './page.mjs';
export default {async fetch(request){const u=new URL(request.url);if(u.pathname==='/api/search'){if(request.method!=='POST')return new Response('Method not allowed',{status:405});if(request.headers.get('origin')&&request.headers.get('origin')!==u.origin)return new Response('Forbidden',{status:403});try{const body=await request.text();if(body.length>3000)throw Error('查詢內容過長。');return Response.json(await search(JSON.parse(body)),{headers:{'Cache-Control':'no-store'}})}catch(e){return Response.json({error:e.message||'查詢失敗，請稍後重試。'},{status:400})}}if(u.pathname!=='/')return new Response('Not found',{status:404});return new Response(html,{headers:{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store','X-Content-Type-Options':'nosniff','Referrer-Policy':'strict-origin-when-cross-origin'}})}};
